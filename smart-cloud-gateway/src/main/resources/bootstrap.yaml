server:
  port: 0

---

spring:
  application:
    name: smart-cloud-gateway
  cloud:
    nacos:
      discovery:
        server-addr: nacos.com:8848
      config:
        server-addr: nacos.com:8848
    sentinel:
      zuul:
        enabled: true



